# 개발일지

> 매일 3줄. 제출 문서의 "반복 개선" 항목 재료다.
> 형식: 오늘 시도한 것 / 실패한 것 / 그래서 바꾼 것

## 2026-08-31
- **시도**: 위시켓 외주 요청문 21건 수집, 손으로 정답지 5건 작성, 2단 프롬프트와 채점 스크립트 작성
- **실패**: 처음엔 도메인별(웹/앱/디자인) 체크리스트를 만들 생각이었으나, 정답지 5건을 만들어 보니 도메인이 달라도 **누락이 나오는 위치는 같았다**. 도메인별 접근은 폐기
- **바꿈**: 체크리스트를 도메인이 아니라 **요청문의 구조적 위치 8개(S1~S8)**로 재설계. 새 업종이 와도 그대로 적용된다

## 2026-08-31 (2차)
- **시도**: 프롬프트 1차 실행 + 채점. 정답지 5건에 대해 stage2를 돌리고 score.py로 재현율 측정
- **실패**: **재현율 100%가 나왔는데 이 숫자는 무효다.** 정답지를 만든 주체가 곧 실행 주체라 답을 알고 푼 셈이다(테스트 오염). 채점 장치가 작동하는 것은 확인했으나 성능 측정은 되지 않았다
- **바꿈**: 이번 실행의 목적을 "점수 측정"에서 "정답지 검증"으로 재정의. 실행 결과에서 **정답지에 없던 18개 항목**을 발견해 정답지에 반영(40 → 58개, high 16 → 19개).
  진짜 baseline은 **정답지를 못 본 실행자**(실제 API 호출)로 측정해야 한다. 그 숫자만 제출 문서에 쓴다

### 이번에 새로 나온 대표 항목
- 회원 비밀번호 해시 이관 실패 시 전 회원 재설정 (여행 커머스) — 한 줄 요구 뒤에 숨은 최대 리스크
- 반려 사유 원문 미제공 자체가 별도 작업 (소개팅앱)
- 격주 시연 8~9회의 준비 공수 (여행 커머스) — S7(기다리는 시간)의 새 유형
- 계약 자동 연장 조항 (소개팅앱) — S8의 새 유형

## 2026-08-31 (3차)
- **시도**: 1차 실행에서 누락이 반복 발견된 S7(기다리는 시간)·S8(못 미루는 날짜) 두 자리를 보강. 실제 사례 5건(격주 시연 8~9회 준비 공수 / 운영 종료 후 데이터 파기·이관 / 계약 자동 연장 조항 / 착수일 미확정인데 완료일만 고정 / 자료 지연 vs 연기 불가 오픈일)을 예시로 넣고, S7에 "이 항목은 일한 시간인가, 묶여 있는 시간인가", S8에 "이 날짜가 밀리면 누가 손해를 보는가"라는 판단 질문을 추가
- **실패**: 원인을 "모델이 S7·S8을 안 봐서"라고 봤는데 1차 출력을 다시 뜯어보니 두 자리 모두 항목을 내긴 냈다. 진짜 문제는 **자리를 안 본 게 아니라 자리의 정의가 좁았던 것** — S7이 "온콜·심사 대기" 예시뿐이라 반복 회의·종료 후 정리·상주 근무처럼 형태가 다른 구속 시간이 그 자리에 매핑되지 않았다. 예시를 늘리는 것만으로는 새 형태가 또 빠지므로 예시 추가는 해법이 아니었다
- **바꿈**: 예시 나열 대신 **자리마다 판단 질문 1개**를 먼저 두고, 예시는 그 질문의 답이 되는 하위 유형으로 묶었다(S7 = 대기/반복 일정/사람 상대/종료 이후/장소 구속 5종). 처음 보는 형태도 질문 한 번으로 자리에 들어오게 하는 구조다.
  남은 리스크: 이 프롬프트가 정답지 5건의 문장을 예시로 인용하게 되어(S1~S6도 원래 그랬다) **그 5건은 더 이상 프롬프트 개선의 baseline 측정용으로 쓸 수 없다.** 실제 API 실행 시 나머지 16건 중 일부를 홀드아웃으로 떼어 측정해야 한다

## 2026-08-31 (3차) — 웹 화면 구축
- **시도**: Next.js로 입력→진행→결과 4블록 화면을 만들고 API 라우트 2개(stage1/stage2)를 붙였다. 빌드·타입체크 통과 후 orca 브라우저로 예시 1건을 끝까지 실행해 확인
- **실패**: 처음엔 두 단계를 API 한 번에 넣으려 했다. 그러면 40~60초 동안 화면이 멈춘 것처럼 보이고, 어느 단계에서 실패했는지도 구분되지 않는다
- **바꿈**: `/api/extract` → `/api/analyze` 두 라우트로 분리하고 클라이언트가 순차 호출하게 했다. 진행 표시가 상태 전환만으로 해결돼 스트리밍 코드가 필요 없어졌고, 실패 지점도 단계별로 드러난다

### 이번에 코드로 굳힌 설계 원칙 2개
- **금액은 서버가 만들지 않는다** — stage2는 공수(시간)만 내고, 단가 곱셈은 `lib/estimate.ts`에서 클라이언트가 한다. "그 금액을 어떻게 믿냐"는 심사 질문에 구조로 답하기 위함
- **키가 없으면 예시 모드로 폴백** — 배포 링크가 항상 무언가를 보여준다. 심사 기간 중 API 장애가 나도 화면이 비지 않는다

### 미검증
- 모바일 화면은 아직 실제 기기에서 확인하지 못했다. 배포 후 폰으로 확인 필요

## 2026-08-31 (4차)
- **시도**: 점수를 재려면 깨끗한 시험 문제가 필요하다고 보고, 남은 16건 중 3건을 홀드아웃으로 떼어냈다(157190 비대면진료 / 157941 여행사ERP / 157864 카드단말기). 길이 4,939·3,320·1,422자, 규제·연동·하드웨어로 유형을 벌려 대표성을 확보
- **실패**: "그 3건은 프롬프트에 인용하지 않는다"는 규칙을 **문서에만 적어두면 반드시 깨진다**. 오늘 S7·S8을 보강하면서 정답지 5건의 문장을 무심코 예시로 넣은 것이 정확히 그 사고였다 — 규칙을 몰라서가 아니라, 예시를 찾다 보면 손이 가장 가까운 요청문으로 가기 때문이다
- **바꿈**: 규칙을 사람 기억에서 **기계 검사로 옮겼다**(`check_holdout.py`). 홀드아웃 요청문을 절 단위로 쪼개 프롬프트 전문과 대조하고, 걸리면 종료 코드 1을 낸다. 검사기 자체가 고장 나 있으면 "정상"만 출력해도 모르므로, 오염이 확실한 157981로 돌려 4건(그중 3건이 오늘 내가 넣은 문장)을 잡아내는 것까지 확인하고 채택

## 2026-08-31 (5차)
- **시도**: 화면이 계속 "구려 보인다"는 피드백. 배포본을 직접 열어 입력·결과 화면을 캡처해 진단하고, 같은 결과 내용을 세 방향(계약 문서 / 판정 계기판 / 작업 체크리스트)으로 그린 시안을 만들어 B안을 채택
- **실패**: 처음엔 "어디가 별로인지" 항목별로 고칠 생각이었다. 그런데 그 방식이 지금까지 "고쳐도 아직 구림"이 반복된 원인 자체였다 — **기준이 없으니 고칠 때마다 다른 데가 어긋났다.** 개별 수정은 해법이 아니었다
- **바꿈**: 코드를 고치기 전에 **방향을 그림으로 확정**하고, 그 방향의 토큰(폰트·색 12개·글자 크기 8단계·간격)을 문서로 먼저 박았다(`docs/화면-디자인-규칙.md`). 이후 수정은 "감"이 아니라 "규칙 적용"이 된다.
  진단에서 나온 최대 원인은 취향이 아니라 사실이었다 — 웹폰트 로드가 아예 없어 보는 사람 기기의 기본 글꼴로 렌더되고 있었고, 색 7개 중 3개가 전부 심각도용이라 화면의 유일한 색이 경고색이었다

## 2026-08-31 (6차)
- **시도**: 규칙 1~4단계를 코드에 적용(폰트 로드·다크 팔레트·흰색 전제 제거·컴포넌트 규칙). 빌드 통과 후 로컬 프로덕션 빌드를 브라우저로 띄워 육안 확인
- **실패**: **모바일이 깨진 것을 하마터면 그대로 배포할 뻔했다.** 폰트를 넣으려고 `layout.tsx`에 `<head>`를 직접 추가했는데, App Router는 layout이 `<head>`를 렌더하면 자동 주입하던 기본 meta(charset·viewport)를 넣지 않는다. viewport가 없으면 모바일이 980px로 렌더돼 화면이 통째로 축소된다.
  **빌드는 통과했고 데스크톱에서는 완전히 멀쩡해 보였다.** "레이아웃 클래스를 안 건드렸으니 반응형은 안전하다"고 추론하고 미검증으로 넘겼다면 그대로 나갔다.
  같은 확인 과정에서 두 번째 사실도 나왔다 — 폰트를 라틴 서브셋만 받고 있어서 **정작 한글은 계속 기본 글꼴로 그려지고 있었다.** 폰트 작업 자체가 헛일이었던 셈이다
- **바꿈**: ① 코드 추론("클래스를 안 건드렸으니 괜찮다")을 근거로 미검증 처리하지 않고, **배포본과 로컬을 같은 명령으로 비교**해 결정적 증거를 만들었다(`curl | grep -c 'name="viewport"'` 가 배포본 1 / 로컬 0). ② 폰트는 `next/font`로 전환. ③ **규칙 문서에 적어둔 방법이 곧 버그의 원인이었으므로 문서부터 고치고 경고와 확인 명령을 넣었다** — 문서를 안 고치면 다음 사람이 같은 실수를 반복한다.
  교훈: "빌드 통과 + 데스크톱 육안 확인"은 완료 증거가 아니다. 바뀐 축(여기서는 화면 폭)을 직접 재야 한다

## 2026-08-31 (7차)
- **시도**: B안의 마지막 단계 — 결과 화면을 계기판 3층(판정 칩 → 금액 대비 → 숫자 4칸 → 목록)으로 재배치
- **실패**: 재배치하고 나서야 두 가지가 드러났다. ① **견적 금액이 목록을 다 지나야 나오고 있었다** — 화면 위아래에 금액이 중복돼 있었는데, 배치를 안 건드리고 색만 바꿨을 때는 문제로 보이지 않았다. ② 제목 자리를 위로 올리자 예시 모드의 제목이 `"예시 요청문"`이라는 게 드러났다. 두 결함 모두 **원래부터 있었지만 배치가 가려서 안 보였던 것**이다
- **바꿈**: 공수·견적을 계기판 안으로 올려 중복을 없애고(권장 금액도 화면에 노출), 제목은 어느 예시인지 대조해 그 이름이 나오게 했다. stage1(제목)과 stage2(분석)가 서로 다른 기준으로 고르면 제목과 내용이 어긋나므로 대조 로직을 `lib/sample-match.ts` 한 곳으로 합쳤다.
  교훈: **레이아웃을 바꾸면 기존 결함이 드러난다.** 색만 바꾸는 수정은 문제를 가린 채로 둘 수 있다

## 2026-09-02 (8차) — 키를 꽂자마자 터진 버그와 과금 방어선
- **시도**: API 키 실사용 전환. 과금 상한을 먼저 확정(선불 크레딧 + 자동충전 OFF = 충전액이 곧 최대 손실)하고, 로컬에서 실분석 1건을 돌려 동작·비용을 실측
- **실패**: **첫 실호출에서 Stage 2가 바로 502로 죽었다.** 출력 상한 8,000토큰에 걸려 JSON이 중간에 잘린 것 — 응답의 output이 정확히 8,000으로 상한과 일치해 원인이 즉시 확정됐다. 그동안의 확인이 전부 예시(mock) 모드였으니 이 버그는 실호출로만 드러난다. 키를 꽂고 공개했다면 첫 사용자가 이걸 만났다.
  비용 실측에서 예상도 하나 뒤집혔다 — 1건 $0.20 중 **88%가 출력 요금**이라, "프롬프트 캐싱이 효과 클 것"이라던 계획은 실측 후 ~8% 절감으로 강등됐다
- **바꿈**: 상한 16,000으로 수정(웹·채점 스크립트 동일 버그 2곳). 방어를 4겹으로 구성 — IP당 시간당 5회(6회째 429 실측), 하루 총 60회 초과 시 예시 모드 폴백(상한을 2로 낮춰 3회째 전환 실측), 입력 6,000자·출력 16,000토큰, 최종선은 선불 크레딧. 배포판에서도 실분석 1건을 완주시켜(69초) 함수 시간제한(300초) 안에 드는 것까지 확인.
  교훈: **mock으로는 API 경계의 버그가 안 나온다. "키 꽂고 1건"이 가장 싼 통합 테스트다**

## 2026-09-02 (9차) — 대기 2분을 "1부 읽는 시간"으로
- **시도**: Stage 2 응답이 실측 69~144초로 편차가 컸다. 진행바만 보여주는 대기를 줄일 방법을 찾음
- **실패(발견)**: 새 기능이 필요한 게 아니었다 — **Stage 1 결과는 이미 10초 만에 브라우저에 도착해 있는데 화면에 안 쓰고 있었다.** API를 2단으로 분리해 둔 구조(3차)의 부산물이 그대로 방치돼 있던 것
- **바꿈**: Stage 1 완료 즉시 "1부 — 요청문에서 읽어낸 것" 카드를 표시하고 최종 결과가 오면 치운다. 사용자는 자기 요청문이 어떻게 읽혔는지 확인하며 기다린다. 실분석으로 표시→소멸까지 확인

## 2026-09-02 (10차) — 첫 진짜 점수: high 재현율 73%
- **시도**: 홀드아웃 3건의 정답지를 만들어 첫 유효 측정. 정답지 초안은 Claude가 쓰되, 법·규제 항목은 외부 근거로 검증(이 과정에서 비대면진료 의료법이 공포되어 **2026-12-24 본사업 시행 + 플랫폼 인증제**라는 사실이 나와 정답지의 뼈대가 됨). 실행 전 check_holdout.py로 오염 없음 확인
- **실패(한계)**: 정답지 작성자가 채점 대상과 같은 계열 AI라는 구조적 한계는 해소하지 못했다. 도메인 전문가 검증도 미완 — 사용자가 "내가 아는 분야가 아니라 판단이 안 된다"고 답한 것이 계기로, 검증을 "사람의 직관"에서 "외부 근거 대조"로 바꿨다. 놓침으로 채점된 7건이 매칭 실패(찾았는데 표현이 다름)인지 전수 대조했고, 7건 모두 진짜 놓침으로 확정
- **바꿈**: **high 재현율 11/15 = 73%** (전체 25/32 = 78%, 판정 일치 3/3)를 첫 기록으로 확정. 놓침 패턴 3종을 분류 — ① 법과 사업 모델의 충돌(유인·알선 금지 vs 수수료), ② 적힌 흐름의 반대편(취소·환불, 사후 단계), ③ 숨은 비용 구조(원격 배포 메커니즘, 사용량 과금).
  오염 규율을 하나 추가: **이 놓침 목록을 보고 프롬프트를 고치면 홀드아웃 3건은 시험 문제로서 무효가 된다.** 개선은 연습용 5건으로만 하고, 개선 효과의 재측정은 남은 요청문 13건에서 새 홀드아웃을 지정해서 한다

## 2026-09-02 (11차) — 2차 홀드아웃 측정과 크레딧 소진 사건
- **시도**: 개선판 프롬프트의 유효 점수를 만들기 위해 남은 요청문 13건에서 새 홀드아웃 3건을 지정(세무 플랫폼 157878 / 네일 AI 앱 157929 / ERP 연동 스케줄러 157885 — 1차와 같은 축을 새 업종으로 대응). 정답지 29항목 작성, 1차 홀드아웃은 golden.json에 소진(burned) 표시
- **실패**: 측정 도중 **선불 크레딧이 바닥나 API가 400으로 멈췄다.** 그런데 이 사고로 설계 구멍이 드러났다 — 잔액 소진은 "키 없음·킬스위치"와 다른 경로라서 예시 모드 폴백을 타지 않고 사용자에게 에러 화면을 그대로 노출한다. 심사 기간에 잔액이 바닥났다면 심사위원이 에러를 봤을 것이다. 또 하나: 연습 5건 기준 측정 중 API 타임아웃(300초 설정)도 실제로 발생 — 실측 최장 291초로 한도가 빠듯했다
- **바꿈**: ① 잔액 소진 오류를 감지해 예시 모드로 넘기는 폴백 추가(단, 검증하려던 순간 충전이 반영돼 실환경 검증은 미완 — 미검증으로 표기) ② 스크립트 타임아웃 600초로. ③ **2차 측정 결과: high 재현율 8/11 = 73%, 전체 23/29 = 79%.** 1차(73%)와 다른 문제에서 같은 수준 — 두 독립 시험에서 일관된 성능. 특히 처음 보는 유형(AI 분석 앱)에서 10/10, 의료기기 경계와 자격 차단(법인만)까지 검출 — 1라운드에 넣은 "사업 모델과 법의 충돌" 판단 질문이 새 도메인에서 작동한 증거.
  단, 1차→2차는 다른 문제라 "개선으로 올랐다"고는 주장하지 않는다(같은 시험을 두 번 보면 오염). 다음 놓침 패턴: 검증 책임 체계 / 상용 패키지 의존 / 멱등성
